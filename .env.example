# Nothing required! The app is fully client-credential driven.
# You paste your GitHub Personal Access Token and CloseRouter API key into
# the app's UI; both are stored only in your browser's localStorage.
#
# Optional override:
# CLOSEROUTER_BASE_URL=https://api.closerouter.dev/v1
